# Log

Append-only record of wiki changes. Newest at the bottom.
Format: `## [YYYY-MM-DD] <init|update|lint> | <short summary>`

## [2026-09-23] init | scanned media-jobs (hybrid: Node/Express web + Python/uv worker over Redis); created index with Components table, architecture + seam contract, per-language conventions, recipes + web/worker/seam module pages
